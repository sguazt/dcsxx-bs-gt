#ifndef DCS_MATH_OPTIM_BRST_GLOBAL_HPP
#define DCS_MATH_OPTIM_BRST_GLOBAL_HPP

namespace dcs { namespace math { namespace optim {

namespace detail { namespace /*<unnamed>*/ {

template <typename RealT>
void brst_solver(std::vector<RealT> const& amin,
				 std::vector<RealT> const& amax,
				 std::size_t m,
				 std::size_t n100,
				 std::size_t ng0,
				 RealT nsig,
				 RealT 
)
{
}

}} // Namespace detail::<unnamed>

}}} // Namespace dcs::math::optim

#endif // DCS_MATH_OPTIM_BRST_GLOBAL_HPP
